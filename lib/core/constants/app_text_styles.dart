/// Referensi Material 3 TypeScale untuk POS Kasir.
/// Gunakan via: `Theme.of(context).textTheme.xxx`
/// JANGAN buat TextStyle manual di widget.
///
/// Scale yang dipakai:
///
/// Display:
///   displaySmall  → 36sp / Regular  — grand total di payment screen
///
/// Headline:
///   headlineLarge → 32sp / SemiBold — judul halaman utama
///   headlineSmall → 24sp / SemiBold — section header, nama produk di detail
///
/// Title:
///   titleLarge    → 22sp / SemiBold — AppBar title
///   titleMedium   → 16sp / Medium   — card title, list item primary
///   titleSmall    → 14sp / Medium   — label, chip text
///
/// Body:
///   bodyLarge     → 16sp / Regular  — body text utama
///   bodyMedium    → 14sp / Regular  — body text sekunder, deskripsi
///   bodySmall     → 12sp / Regular  — caption, hint text
///
/// Label:
///   labelLarge    → 14sp / Medium   — tombol
///   labelMedium   → 12sp / Medium   — badge, tag
///   labelSmall    → 11sp / Medium   — timestamp, metadata kecil
///
/// Font: Plus Jakarta Sans (via google_fonts)
// ignore_for_file: unused_field
class AppTextStyles {
  AppTextStyles._();
  // Kelas ini hanya dokumentasi — lihat app_theme.dart untuk implementasi
}
